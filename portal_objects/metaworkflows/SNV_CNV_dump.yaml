name: Somatic Drivers to JSON
description: Save driver mutations (SNVs, INDELs, CNVs) in a JSON file.

input:

  vcf:
    argument_type: file.vcf_gz
    dimensionality: 1
  
  cnv:
    argument_type: file.tsv_gz
    dimensionality: 1 
   
workflows:

  dump_drivers:
    input:
      vcf:
        argument_type: file.vcf_gz
        input_dimension: 1
      cnv:
        argument_type: file.tsv_gz
        input_dimension: 1
    
    output:
      driver_json:
        description: JSON file containing driver mutations for SNVs, INDELs and CNVs
        file_type: GosCan compatible JSON
    
    config:
        instance_type: c5.4xlarge
        ebs_size: 1.6x
        EBS_optimized: True
        spot_instance: False
        run_name: run_driver_annot
        behavior_on_capacity_limit: wait_and_retry
      

      



  

  
  

  
